/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.infigon.app'], // Add the domain from which you want to load images
    },
  }
  
  module.exports = nextConfig
  